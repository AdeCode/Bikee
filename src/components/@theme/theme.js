import { createTheme, ThemeProvider, styled } from '@mui/material/styles';

export const theme = createTheme({
    palette:{
        lightBlue: '#556987',
        white:'#FFFFFF',
    },
    secondary:{
        main:'#f44336',
    }
})